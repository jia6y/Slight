require 'slight/tilt'

script = %q{
	div "btn btn-succes #{btn_size}" do 
		@btn_txt	
	end
}

@btn_txt = 'Click Me'
body = Proc.new { script }

template = Tilt.new('tilt_example.rb', 6, {}, &body)
puts template.render(self, :btn_size => "btn lg")
